# ================================================================
#                   CONFIGURAÇÃO DO SERVIDOR
# ================================================================

# Porta em que o servidor irá rodar
PORT=3000

# ================================================================
#                   SEGURANÇA E SESSÕES
# ================================================================

# Chave secreta para criptografia das sessões
# IMPORTANTE: Gere uma chave forte e única para produção
# Você pode usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=seu-segredo-super-secreto-aqui

# ================================================================
#                   FIREBASE / FIRESTORE
# ================================================================

# ID do projeto Firebase (encontrado no console do Firebase)
FIREBASE_PROJECT_ID=seu-projeto-id

# Email da conta de serviço (Service Account)
# Obtido em: Firebase Console > Project Settings > Service Accounts
FIREBASE_CLIENT_EMAIL=seu-email@seu-projeto-id.iam.gserviceaccount.com

# Chave privada da conta de serviço
# ATENÇÃO: Mantenha esta chave em segredo! Não commite no Git!
# A chave deve estar entre aspas e incluir as quebras de linha (\n)
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nSUA_CHAVE_PRIVADA_AQUI\n-----END PRIVATE KEY-----\n"

# ================================================================
#                   OBSERVAÇÕES
# ================================================================

# 1. Copie este arquivo para .env e preencha com seus valores reais
# 2. NUNCA commite o arquivo .env no Git (já está no .gitignore)
# 3. Para obter as credenciais do Firebase:
#    - Acesse o Firebase Console
#    - Vá em Project Settings > Service Accounts
#    - Clique em "Generate New Private Key"
#    - Baixe o arquivo JSON e extraia os valores
